import {MatchLookup} from '@/features/matches/match-lookup';
export const metadata={title:'Find a match'};
export default function Page(){return <div style={{maxWidth:680,margin:'0 auto'}}><div className="eyebrow">MATCH LOOKUP</div><h1 style={{marginTop:14}}>Find a match</h1><MatchLookup/></div>}
